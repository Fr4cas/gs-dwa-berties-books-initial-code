<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What we sell at <%=shopName%></title>
</head>
<body>
    <h1><%=shopName%> Shop</h1>
    <h2>Here are some bargain books:</h2>
    <ul>
        <% for(let i = 0; i < availableBooks.length; i++) { %>
            <li><%= availableBooks[i].name %>, <%= availableBooks[i].price %> </li>
        <% } %>
    </ul>
</body>
</html>